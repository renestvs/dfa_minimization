from completion import complete;

function complement(A)
   let compA := complete(A);

   foreach state in compA.getStates() do
      compA.toggleFinalState(state)
   done

   return compA
end function

run(complement, get_automaton(currentAutomaton));

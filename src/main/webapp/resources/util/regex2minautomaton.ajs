from determinization import determinize;
from minimization    import minimize;
from regex2automaton import regexToAutomaton;
from normalization   import normalize;

run(
   function()
      return normalize(minimize(determinize(regexToAutomaton())));
    end function
);
